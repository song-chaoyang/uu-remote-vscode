# Changelog

## 0.8.3

- **远程终端降级可用**:旧版 CLI(无 `term --device-id` 管道通道)下,「打开远程终端」/「新建会话」
  不再只是警告——自动 fallback 到 `term <device-id>`(实测旧 CLI 该命令请求主程序
  打开终端窗口并返回 success:true),点一下就能在主程序中连上远程终端,并提示升级可获得 VSCode 内嵌终端。
- 会话列表/附加/终止在旧 CLI 上仍引导升级(无法降级)

## 0.8.2

- **CLI 版本兼容层(适配旧版 uuyc-cli / macOS UURemote 4.39.x 自带 CLI 1.0.0)**:
  - `echo` 输出兼容:旧版 CLI 返回 JSON 信封,自动解包 `data.message`(新版纯文本照常)
  - 本机设备 ID:旧版无 `-d`,自动回退 `assist id`;新版保留原路径
  - 自定义验证码:旧版无 `--reset-custom-code`,自动回退 `assist set-code`
  - `lterm ls`:旧版输出 JSON、新版输出 TSV 表格,双格式解析
  - `device status`:兼容旧版 `connections` 字段(新版为 `connected_devices`)
  - 平台字段:兼容字符串平台("windows"/"mac"/"linux")与数字(1/4=Windows)
  - **能力探测 + 优雅降级**:启动前探测 term 管道通道 / input-diag / 码率与连接模式等能力(按 CLI 路径缓存)。
    旧版 CLI 上,远程终端、文件传输、附加到编辑器、AI 远程执行/PTY 工具快速失败并给出升级引导,
    不再挂 20 秒超时或报无意义的 "Unknown option"
  - macOS 主程序识别补全 `UURemote.app`(仅识别 UU远程.app / GameViewer.app 时找不到主程序)
- 冒烟测试:能力探测断言 + 错误检测断言自适应新旧 CLI;TermBridge 失败路径诊断覆盖新提示
- 冒烟测试(真机)与集成测试全通过

## 0.8.1

- 无线副屏入口增强:设备树新增「无线副屏」常驻入口节点(用户节点下方,单击打开引导面板,右键:打开引导/启动主程序/官方介绍)

## 0.8.0

- **无线副屏引导**:命令面板/设备右键「无线副屏」——Webview 引导面板(功能特性、使用步骤、布局方向提示)+ 一键启动主程序直达副屏入口 + 官方介绍/下载页直达(功能由 UU远程主程序提供,插件负责引导与入口)
- 命令总数 55
## 0.8.0

- **无线副屏**:新增无线副屏引导面板(Webview)与快捷命令,将闲置手机 / 平板通过 UU远程变身为电脑的高性能无线触控副屏
  - 侧边栏标题栏「📱」按钮一键打开引导面板(含操作步骤、功能特性、一键启动主程序)
  - 右键设备 → 「启动无线副屏(主程序)」快捷入口
  - 命令面板:「打开无线副屏」/「启动无线副屏(主程序)」/「无线副屏官方介绍」
  - 引导面板内嵌:启动主程序、查看官方介绍、下载手机端 UU远程
  - 命令总数 54

## 0.7.0

- **文件传输**(自研 TermBridge,CLI 未提供该能力):右键设备「上传文件到设备 / 从设备下载文件」,走 VSCode 原生文件选择/保存对话框;上传 ≤512KB、下载 ≤256KB(通道吞吐 ~5KB/s),更大文件引导使用主程序
- **远程启动程序**(桌面端快速启动能力的命令行版):右键设备「启动程序」,内置记事本/任务管理器/计算器/资源管理器/cmd,也可输入任意路径(Start-Process);AI 工具 uu_remote_launch(双路,工具总数 16)
- **重命名设备入口**:右键「重命名设备」——CLI 未提供 rename,一键引导到主程序操作(与退出登录同策略)
- **云电脑开机诊断增强**:开机请求后弹窗显示 CLI 完整响应;5s/15s/40s 三次轮询,60 秒仍为关机状态时明确警告并指向日志(响应原文含配额/时段/会员限制等真实原因)
- **端口映射**:CLI 未暴露该能力(主程序功能),README 能力矩阵中标注
- 命令总数 51
## 0.6.4

- 防御修复:命令选择对象收紧 —— ID 为空/缺失的对象不再被当作有效目标执行(修复日志中「连接设备 undefined(undefined)」),无效对象会回退设备选择
- 日志确认:交互式远程终端与会话附加(--session-id)通道正常;21:38 的「主控端版本过低」为环境问题,其诊断信息自 0.6.3 起会完整透出
## 0.6.3

- **修复:会话断开错误不带诊断**——「远程终端会话已断开」现在会附上被控端返回的关键错误(如「主控端版本过低,请升级主控端」),让用户看到真实原因而非无信息的断开提示;写入路径同样处理
- 冒烟测试的 TermBridge 真机段改为自适应:通道可用时验证功能,不可用时验证错误消息必须携带诊断
## 0.6.2

- **修复「附加到编辑器」误报**:platform 1 与 4 均实测为 Windows,移除过窄的 platform 前置拦截(platform 4 的设备此前被误判为非 Windows)
- 连接失败时给出带上下文的诊断(主控端版本过低 / 非 Windows 被控端两种原因 + 下载页入口)
## 0.6.1

- **关键修复:树视图右键菜单不显示**:VSCode 1.136 起树视图右键菜单位置为 `view/item/context`(此前声明的 `view/item` 在该版本不再渲染右键菜单),现已双位置声明(`view/item` + `view/item/context`)兼容全版本,覆盖 33 条菜单:设备(在线/离线)、云电脑、用户节点、分组标题、状态引导节点
- **远程终端退出诊断**:集成终端异常退出(如退出码 6 = 本机主控端版本低于被控端)时弹出明确升级指引
- 集成测试新增「树节点运行时 contextValue 与菜单声明一致」断言,防止右键菜单回归

## 0.6.0

- **交互式终端原语**(参考 uu-pty-bridge 生态,同名工具语义对齐):新增 `uu_pty_open` / `uu_pty_send` / `uu_pty_read` / `uu_pty_close` 四个 AI 工具(MCP + Copilot 原生工具双路)
  - 解决 AI 执行交互式命令会挂死的问题:ssh 密码提示、y/n 确认、分页输出等场景现在可通过 open → send(`\r` 回车)→ read 循环处理
  - 交互会话空闲 30 分钟自动释放;close 发送 exit 正常结束远程会话
  - 典型链路:`uu_pty_open` → `ssh user@内网主机` → 密码提示时 `uu_pty_send` 回答 → 执行集群命令(穿越内网)
- AI 工具总数 15(11 基础 + 4 交互原语)
- 备注:uu-pty-bridge 认为 term 通道「管道必挂须 node-pty」,本插件的管道模式在 CLI 4.39.2 实测多轮全绿(含文件读写),保持零依赖实现;如遇兼容性问题再评估 PTY 方案

## 0.5.0

- **易用性大升级**:
  - 右键菜单全节点覆盖:设备(在线/离线)、云电脑、用户节点、分组标题、状态引导节点全部有右键菜单;`when` 子句全部改为精确匹配(防御性)
  - **单击设备直达详情面板**(Webview);悬停显示内联按钮(连接/远程终端/详情)
  - 用户节点右键新增 **「退出登录」**(登录态由主程序管理,引导在其中完成退出)
  - **CLI 未安装引导**:状态节点点击 / 首次激活自动弹出「下载安装 UU远程(官网 uuyc.163.com/download)/ 设置 CLI 路径 / 查看日志」
- **Mac / Windows 双平台适配**:
  - macOS 自动探测 CLI 路径(`/Applications/UU远程.app` 等)+ `open -a` 启动主程序
  - TermBridge **shell 参数化**:远程执行支持 powershell / zsh / bash 三套协议(冲刷、哨兵、分页、base64);AI 工具 `remote_exec` 等新增 `shell` 参数
  - 「附加到编辑器」非 Windows 被控端给出明确引导(虚拟文件系统暂仅支持 Windows)
  - 设备树显示平台名(Windows / platform N)
- **诊断增强**:远程会话握手失败时透出被控端错误(如「主控端版本过低,请升级本机 UU远程」)
- MCP / LM 工具描述更新(平台指引);命令总数 47

## 0.4.0

- **AI 集成(安装即用,零配置)**:
  - 内嵌 **MCP 服务器**(stdio):11 个工具供 Copilot agent 模式及其他 MCP 客户端调用,基于 `contributes.mcpServerDefinitionProviders` + `vscode.lm.registerMcpServerDefinitionProvider` 自动注册,复用编辑器内置 Node
  - **Copilot 原生 Language Model Tools** 11 个(`#uuDevices`、`#uuExec`、`#uuRead`、`#uuWrite` 等),与 MCP 共享同一套工具后端(src/tools.mts)
  - 工具集:列出设备、远程执行 PowerShell、远程列目录/读/写文件、串流连接/断开、云电脑列表/开关机、通信测试、本机 ID
  - 危险操作带 annotations(readOnly/destructive 标注),由 VSCode 确认 UI 把关
  - 冒烟新增 MCP stdio 协议实测(initialize / tools/list / tools/call 真实调用)
- 升级 @types/vscode 至 1.136、engines.vscode 至 ^1.99.0(适配 MCP 与工具 API)
- 集成 @modelcontextprotocol/sdk 1.30(bundle 进 dist/mcp-server.cjs,vsix 无运行时依赖)

## 0.3.0

- **附加到编辑器**:右键在线设备 → 在新窗口以 `uu-remote://` 虚拟文件系统打开远程目录,像 Remote-SSH 一样浏览/编辑/保存远程文件
  - 自研 TermBridge:基于 `uuyc-cli term` 通道的编程化远程执行(ANSI 屏幕模拟 + 哨兵协议 + 分页拉取 + base64 传输)
  - 支持 stat / 读 / 写 / 新建目录 / 删除 / 重命名;路径经 PowerShell 单引号转义防注入
  - 通道空闲 5 分钟自动正常结束会话(exit),不残留远程会话
  - 限制:仅 Windows 被控端;读 ≤256KB、写 ≤512KB(通道吞吐 ~5KB/s)
- 远程终端会话列表改为真实 TSV 表格解析(SESSION_ID/NAME/SHELL/STATE/LAST_ACTIVE),QuickPick 显示 Shell / 状态 / 活跃时间
- 设备右键菜单新增「附加到编辑器」;设备详情面板(Webview)新增同名按钮
- 新增 VT 屏幕模拟器与真实字节流 fixture 回归测试、TermBridge 真机只读冒烟测试(opt-in)
- 命令总数 44

## 0.2.0

- 可视化交互大升级:
  - 设备右键菜单扩充:串流连接、打开远程终端、新建远程会话、会话列表、设备详情、复制设备 ID、断开(在线设备另有连接 / 终端 / 详情内联按钮)
  - 云电脑右键菜单扩充:开机、关机、连接、断开、详情、复制 ID
  - Webview 详情面板(设备 / 云电脑):跟随 VSCode 主题,内嵌串流 / 终端 / 开关机 / 复制等操作按钮,操作后自动刷新状态
  - 设备树新增用户节点(昵称 / VIP,右键:用户详情、钱包余额、复制用户 ID)与在线 / 离线分组
  - 状态引导节点:主应用未运行或未登录时显示一键「启动主程序」;CLI 未找到时一键打开设置
  - 状态栏点击升级为 QuickPick 状态面板:已连接设备二级操作(终端 / 详情 / 断开)+ 连接新设备、断开全部、刷新
  - 视图标题栏按钮:设备视图(刷新、连接、状态)、云电脑视图(刷新、开机)
- 云电脑支持「全部」关机 / 断开(CLI 不带 ID 语义)
- 新增 `lterm has` 会话检查命令
- 命令总数 43

## 0.1.0

- 初始版本
- 侧边栏「UU远程」:设备列表与云电脑列表树视图(在线 / 运行状态、内联按钮、右键菜单)
- 状态栏显示已连接设备数,点击查看详情
- 设备:连接 / 断开 / 全部断开 / 连接状态 / 本机设备 ID / 重置自定义验证码
- 云电脑:开机 / 关机 / 连接 / 断开 / 列表刷新(关机与断开支持「全部云电脑」)
- 远程终端:打开(powershell/cmd/zsh/bash)、新建会话、会话列表、附加、终止
- 本地 UU 终端会话:新建 / 附加 / 列表 / 检查 / 终止 / 重命名,并提供终端 Profile「UU 本地终端」
- 用户:用户信息、钱包余额
- 设置:码率上限、Lite Punch 连接模式
- 诊断:echo 通信测试、版本信息、输入诊断开关/导出、键盘钩子重装、Win 键探测
- 输出通道「UU远程」记录全部 CLI 调用;CLI 路径自动探测 + 可配置
